### Example Python Rock-Paper-Scissor game

Example for refacturing from single file into functions into class.

### Containerized usage

```
docker build -t <image_name> .
docker run -it <image_name>
```
